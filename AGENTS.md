# 天气查询应用开发指令

## 项目概述

使用 React + TypeScript + Vite + Tailwind CSS 开发的天气查询应用（Weather Today）。
用户输入城市名称即可查询当前天气与未来几天的天气预报，应用支持自动获取用户位置、收藏常用城市。
天气数据来自 OpenWeatherMap 免费 API，收藏城市保存在 LocalStorage。

## 开发规范

- 使用函数式组件 + Hooks
- 使用 Tailwind CSS 编写样式
- 组件要可复用
- 代码要有注释
- 使用 TypeScript，天气数据接口统一定义在 types 目录，不要使用 any

## 设计要求

- 简洁现代的设计风格
- 使用渐变色作为强调色
- 天气状况与图标对应（晴、多云、雨、雪等）
- 查询过程中显示加载动画
- 确保移动端适配（响应式布局）

## 注意事项

- 保持设计简洁，不要过度设计
- API Key 必须通过环境变量读取（.env.local），禁止硬编码在代码中
- .env.local 必须加入 .gitignore，不得提交到 Git，避免泄露
- 所有 API 错误（城市不存在、API Key 无效、网络错误）必须给出友好的中文提示
- 自动定位失败时降级为手动输入搜索，不阻塞主流程
- 收藏城市使用 LocalStorage 持久化，读取失败时要容错为空列表
