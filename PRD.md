# 天气查询应用 PRD

## 产品概述

一款基于 OpenWeatherMap 免费 API 的天气查询应用（Weather Today）。用户输入城市名称即可查看当前天气与未来几天的天气预报，应用会通过浏览器定位自动获取用户位置并展示本地天气，支持收藏常用城市以便下次快速查询。

## 核心功能

1. 城市天气查询：用户输入城市名称（支持中文/英文），查询并显示当前天气，包括温度、天气状况、体感温度、湿度、风速、气压等信息
2. 未来天气预报：展示未来 5 天的天气预报，包括日期、天气状况、最高/最低温度
3. 自动定位：进入应用时通过浏览器 Geolocation API 自动获取用户位置并展示当地天气；用户拒绝授权或定位失败时降级为手动输入
4. 城市收藏：一键收藏/取消收藏当前城市，收藏列表保存在 LocalStorage，下次打开应用可直接点击快速查询
5. 加载与错误提示：查询过程中显示加载动画；城市不存在、API Key 无效、网络异常等错误均给出友好的中文提示

## 交互流程

1. 打开应用 → 尝试自动定位
   - 定位成功 → 展示本地天气
   - 定位失败或用户拒绝 → 显示默认城市，引导用户手动搜索
2. 输入城市名并搜索 → 显示加载动画 → 展示当前天气 + 未来几天预报
3. 点击收藏按钮 → 当前城市加入收藏列表（LocalStorage 持久化）
4. 点击收藏列表中的城市 → 直接查询该城市天气

## 设计要求

- 简洁现代的设计风格
- 使用渐变色作为强调色
- 天气状况与图标对应（晴、多云、雨、雪等）
- 加载动画与错误提示设计友好
- 移动端适配（响应式布局）
